services:
  postgres:
    image: postgres

    container_name: draw-postgres

    restart: always

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: drawdb

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: